You may also wish to run git status after you have created the file.
You should see that now both files are listed as untracked files.

When you've created and updated your reflections document for lesson 2,
click "Next."

What happens when you initialize a repository? Why do you need to do it?

It creates an empty repository for the reflections files.
This consists of a hiddendirectory called .git

How is the staging area different from the working directory and the
repository?  What value do you think it offers?

The staging area is where you "store" files to be placed in the
repository.  In the working directpy are all the files needed for the
project or task at hand whether they are being editted or not.

How can you use the staging area to make sure you have one commit per
logical change?

In the staging are you store the files you have modified so they can be
stored under one comit.

What are some situations when branches would be helpful in keeping
your history organized? How would branches help?

When it comes to experimenting.

How do the diagrams help you visualize the branch structure?

Helps to see what concurrent efforts are going on in a project.

What is the result of merging two branches together? Why do we
represent it in the diagram the way we do?

Updates from both branches are now one.

What are the pros and cons of Git's automatic merging vs. always
doing merges manually?

Auto:
	pro:	speed, ease of use
	con:	leave updates uo to git

Manual:
	pro:	you know hat updates go where
	con:	slow, tedious
